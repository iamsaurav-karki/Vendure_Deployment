apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: saurav-shop
data:
  APP_ENV: "production"
  VENDURE_SERVER_PORT: "3000"
  DB_HOST: "postgres"
  DB_PORT: "5432"
  DB_NAME: "vendure"
  DB_SCHEMA: "public"
